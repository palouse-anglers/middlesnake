% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_geoserver_layer.R
\name{get_geoserver_layer}
\alias{get_geoserver_layer}
\title{Load a vector (WFS) or raster (WCS) layer from GeoServer}
\usage{
get_geoserver_layer(
  layer_name,
  workspace = "Columbia",
  base_url = "geoserver.megaloptera-data.com/geoserver",
  raster = FALSE
)
}
\arguments{
\item{layer_name}{Name of the layer (without workspace prefix)}

\item{workspace}{Workspace name (default: \code{"Columbia"})}

\item{base_url}{Base WFS URL (default: Columbia GeoServer endpoint)}
}
\value{
An \code{sf} object representing the requested vector layer, or \code{NULL} with a warning if the request fails.
}
\description{
This function retrieves a GeoServer WFS (Web Feature Service) vector layer as an \code{sf} object.
It assumes the GeoServer layer is served in GeoJSON format.
}
\section{GeoServer Protocol Comparison}{
\tabular{lllll}{
   Protocol \tab Full Name \tab Purpose \tab Typical Data \tab Access with \cr
   \strong{WFS} \tab Web Feature Service \tab Retrieve vector features + attributes \tab Shapefiles, GeoJSON \tab \code{sf::st_read()} \cr
   \strong{WMS} \tab Web Map Service \tab Display map tiles (no analysis) \tab Rendered image tiles \tab \code{leaflet::addWMSTiles()} \cr
   \strong{WCS} \tab Web Coverage Service \tab Access raster data for analysis \tab GeoTIFF, NetCDF, imagery \tab \code{terra::rast()} \cr
}


Use this function when you need \strong{vector data} for analysis. For raster datasets (e.g., USDA cropland,lidar), use a WCS endpoint and the \code{terra} package.
}

\examples{
\dontrun{
  get_geoserver_layer("all_columbia_huc_12s")
}
}
